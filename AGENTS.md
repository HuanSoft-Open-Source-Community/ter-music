# 规则

## 文档多语言同步

如需更新文档，请不要漏掉该文档的任何语言版本（如果有），哪怕是先秦版。
例如 README 有英文、简体中文、先秦文言时，三份都必须同步更新。
如果该文档没有其他语言版本，则忽略此规则。

## git规则
人工智能不得擅自执行对仓库的操作（查看除外）。每次对仓库的操作都要由人类确认。
如果需要执行提交操作，请使用标准化的Commit语句和英文。
在提交之前，建议备份diff内容到/tmp/diff-back/diff-<timedate>.bak
如果遇到大量的文件变更，必须严格理解每个变更改了什么，commit不得瞎编。
关于提交有拆分提交和单次提交两个选择，必须权重给出最优解，并让用户选择。
本规则只在代码由人类验收通过时才可执行。其他场景无需理会。

## 八荣八耻
请你在本会话中遵守以下规则（工程八荣八耻）：
以瞎猜接口为耻，以认真查询为荣
以模糊执行为耻，以寻求确认为荣
以臆想业务为耻，以人类确认为荣
以创造接口为耻，以复用现有为荣
以一遍通过为耻，以回头校验为荣
以破坏架构为耻，以遵循规范为荣
以假装理解为耻，以诚实无知为荣
以盲目修改为耻，以谨慎重构为荣
在关键回答后，请附上 自检结果，格式如下：
自检结果 ✅
接口来源确认：……
需求确认：……
业务确认：……
接口复用：……
结果校验：……
架构规范：……
理解诚实：……
修改谨慎：……
